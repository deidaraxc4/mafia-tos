# mafia-tos
play the mafia party game

# server
To start the server, run `npm run start`

# client
To start the client, run `npm run dev`

To create a production build, run `npm run build` which will output the `dist` folder